package SolidPrinciples.LSP;

public class Main {
    /**
     *  Liskov Substitution Principle (LSP) :-
     *  It applies to inheritance in such a way that derived
     *  classes must be completely substitutable for their base
     *  classes. In other words, if class A is a subtype of class B,
     *  then we should be able to replace B with A without
     *  interrupting the behaviour of the program.
     */
}
